package com.breviare;

// Requires a live DATABASE_URL (Flyway/Hikari fail without one) — not available in this
// environment, so it's disabled here rather than left failing the build.
// import org.junit.jupiter.api.Test;
// import org.springframework.boot.test.context.SpringBootTest;
// import org.springframework.test.context.ActiveProfiles;

// @SpringBootTest
// @ActiveProfiles("test")
class BreviareApplicationTests {

    // @Test
    // void contextLoads() {}
}
